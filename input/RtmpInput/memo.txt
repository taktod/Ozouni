メモ

現状のrtmpInputモジュールでは、rtmpのデータはflvとして扱っています(内部的に)
これをmkvベースにすることができたら、timestampがintからlongになるので良い感じになると思われます。
考慮すべし

また現状のrtmpInputモジュールでは、映像と音声のデータソートが独立して動作していますので、注意が必要(映像側のデータと音声側のデータが同期とれていない場合があり得ます。)